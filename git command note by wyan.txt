#git initial setup
git init
git config --global --list
git config --global user.name "NAME"
git config --global user.email "EMAIL"

#some changes and modified fies
git add FILE_NAME
git add .
git status
git commit -m "MESSAGES"

#branch reviewing and switching
git branch -a
git checkout BRANCH_or_COMMIT
git branch NEW_BRANCH_NAME
git checkout -b NEW_BRANCH_NAME
git branch -D branch

#remote repo
git remote add origin REMOTE_REPO_URL_and_REPO_CREDENTIAL
git remote -v
git push origin BRANCH_NAME
git merge what_u_want_to_marge_in_current_repo
git fetch
git pull oringin master

#checking log
git log
git log --online
git log --online | wc -l
git --shortlog
git log BRANCH_NAMES
git log --name-only

#redo/undo
git revert COMMIT_ID
git reset --soft HEAD~1
git reset --hard HEAD~1
